scripted_gui = {

	coc = {
		context_type = player_context
		
		window_name = "coc_container"
        parent_window_token = politics_tab 

		visible = {
			original_tag = GRE
            always = yes
		}
		
		effects = {
			coc_open_button_click = {
				if = {
					limit = { NOT = { has_country_flag = coc_opened } }
					set_country_flag = coc_opened
				}
				else = {
					clr_country_flag = coc_opened
				}
			}
		}
		
		triggers = {
			coc_open_button_visible = {
			}
		}
		
		ai_test_interval = 300
		ai_test_variance = 0.1
		ai_max_weight_taken_per_test = 2
		
		ai_enabled = {
            always = no
		}
		
		ai_weights = {
			coc_open_button_click = {
				weight = 1.0
				ai_will_do = {
					factor = 1
				}
			}
			coc_open_button_max_click = {
				weight = 1.0
				ai_will_do = {
					factor = 1
				}
			}
		}
	} 
}

